<h1>ユーザー一覧</h1>
 <div class="profile_list">
   <table>
     <theader>
     </theader>
     <tbody>
       <% @users.each do |user| %>
         <tr>
           <td>
             <%= link_to(@user.username, "/pages/#{user.id}") %>
           </td>
         </tr>
       <% end %>
     </tbody>
   </table> 
</div>  